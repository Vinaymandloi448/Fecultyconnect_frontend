/**
 * DashboardPage — thin page shell
 *
 * Composes feature-specific components for the dashboard view.
 * Keep business logic in features/ and reusable UI in components/.
 */

export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-white">Dashboard</h1>
      {/* Compose dashboard widgets here */}
    </div>
  );
}
