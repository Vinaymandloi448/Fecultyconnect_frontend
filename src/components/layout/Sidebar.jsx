/**
 * Sidebar placeholder
 *
 * Will contain navigation links, user avatar, logout button.
 * Keep this file focused — each nav item can be its own small component if it grows.
 */

export default function Sidebar() {
  return (
    <aside className="hidden w-64 flex-col border-r border-slate-800 bg-slate-900 md:flex">
      {/* Navigation links will go here */}
    </aside>
  );
}
